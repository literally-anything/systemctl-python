from setuptools import setup

setup(
        name = 'systemctl-python',
        version = '1.0',
        packages = ['systemctl'],
        url = '',
        license = '',
        author = 'Hunter Baker',
        author_email = 'hunterbaker@me.com',
        description = ''
)
print("To use the start() and stop() methods, your user must be in the sudoers file with NOPASSWD")
